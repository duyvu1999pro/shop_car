@model IEnumerable<ShopCar.Model.CTPhieuNhap>

<table class="table">
    <tr>
        <th>
           Số lượng
        </th>
        <th>
            Đơn giá
        </th>
       
        <th>
            Tên sản phẩm
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.SoLuong)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.DonGia)
    </td>

    <td>
        @Html.DisplayFor(modelItem => item.SanPham.TenSP)
    </td>
    @*<td>
        <input type="hidden" name="MaPN" value="@item.MaPN">
    </td>*@
        <td>
                @Html.ActionLink("Sửa", "Edit1", new { id = item.MaPN, id2=item.MaSP }) |

                @Html.ActionLink("Xóa", "Delete1", new { id = item.MaPN, id2 = item.MaSP })
            </td>
</tr>
}

</table>

@model IEnumerable<ShopCar.Model.LoaiSP>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

    <h2>Quản lý các loại sản phẩm</h2>

<p>
    @Html.ActionLink("Tạo mới", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Mã loại
        </th>
        <th>
            Tên loại
        </th>
        <th>
            @* @Html.DisplayNameFor(model => model.MoTa)*@
            Mô tả
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.MaLoaiSP)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.TenLoai)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.MoTa)
    </td>
    <td>
        @Html.ActionLink("Sửa", "Edit", new { id = item.MaLoaiSP }) |
        @Html.ActionLink("Xem", "Details", new { id = item.MaLoaiSP }) |
        @Html.ActionLink("Xóa", "Delete", new { id = item.MaLoaiSP })
    </td>
</tr>
}

</table>
